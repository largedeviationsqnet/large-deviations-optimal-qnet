import StateDepMOR.PaperStatements

/-!
# Main theorem

The former conditional `PerformanceSemantics` proof spine has been removed.
The concrete proof of `PaperStatements.MainTightStatement` is assembled in
`StateDepMOR.MainAssembly` from the proved fixed-state and universal bounds.
-/
